# Firefly Studio 前端体验优化 TODO

目标：让本地创作台更像一套可靠的创作工具，而不是只展示 API 表单。优先改善任务状态、成片工作流、模型选择、移动端和可访问性。保持 React + 现有 CSS，不增加 UI 框架或状态管理依赖。

## 约束

- 保持当前深色、青绿色强调色和紧凑工作台语言，不重做品牌。
- 先复用 `App.jsx`、`App.css` 和现有组件；不引入组件库。
- 所有新状态必须有 loading、empty、error、success / partial 状态。
- 保证桌面和窄屏可用；不能依赖 hover 才能完成关键操作。
- 保留键盘操作和 `prefers-reduced-motion` 兼容。
- 不在前端伪造任务成功；以后端任务状态和最终产物为准。

## P0：成片任务反馈

### 1. 明确区分成片成功、部分成功和失败

文件：`frontend/src/App.jsx`、`frontend/src/components/video.jsx`、`frontend/src/App.css`

现状：

- `failed` 视频任务也会进入 `ResultView`。
- 页面只显示“最终视频文件不可用”，没有解释是否还有可查看的分镜。
- 后端已可能返回“分镜已生成，最终合成失败”的任务 message，但 UI 没有突出展示。

修改：

- `ResultView` 根据 `job.status` 显示三个清晰状态：
  - `succeeded`：最终成片播放器、总时长、分镜缩略图、清单链接。
  - `partial` 或“有分镜但没有 finalVideoUrl”：黄色/中性失败说明、任务 message、可查看分镜和 manifest。
  - `failed` 且无分镜：错误状态、任务 message、重新生成按钮。
- 在结果区顶部增加状态摘要，避免用户在无播放器时误解任务完成。
- 显示可读错误数量，不直接将技术堆栈大段渲染在主界面。
- “再生成一条”保留；部分失败时额外提供“查看分镜清单”。

验收：

- 无 final MP4 时不出现成功语义。
- 有分镜但合成失败时，用户能知道已保留哪些结果。
- 失败任务在手机窄屏不溢出。

### 2. 让成片进度真正可扫读

文件：`frontend/src/components/video.jsx`、`frontend/src/App.css`

现状：

- 当前进度只有一条百分比和纯文字镜头列表。
- 分镜生成期间看不到已完成镜头的视觉产物。
- `stage="concatenated"` 没有对应中文状态文案。

修改：

- 进度页为已完成镜头显示视频 poster 或抽帧缩略图；无缩略图时显示统一占位。
- 每个镜头行显示：镜头序号、状态、当前阶段、时长、简短旁白。
- 补齐阶段映射：`planned`、`video`、`tts`、`done`、`concatenated`、`failed`。
- 将“生成第 X/N 镜”与全局进度放在同一区块，数字使用 tabular numerals。
- 任务长时间无更新时，显示“仍在等待上游返回，可关闭面板后继续”的非错误提示。

验收：

- 用户一眼能知道当前镜头、已完成镜头和失败镜头。
- 3 到 6 个镜头时，窄屏不产生横向滚动。
- 不在每次轮询时导致列表明显跳动或滚动位置重置。

### 3. 从普通任务列表进入成片详情

文件：`frontend/src/components/chat.jsx`、`frontend/src/App.jsx`

现状：

- 成片任务显示在普通聊天线程中，metadata 为空或不完整。
- 侧边“最近任务”点击只聚焦聊天条目，不能打开成片详情。

修改：

- `kind === "video_pipeline"` 的任务使用专门卡片或明显的“成片任务”标识。
- 在聊天线程的成片任务上提供“查看成片进度/结果”按钮。
- 在最近任务点击成片任务时，打开一键成片面板并恢复该任务；普通图/视频任务保持现有聚焦行为。
- 不新增路由；复用已有 `openVideoPanel` 和 `videoJob` 恢复逻辑。

验收：

- 刷新后可从聊天和侧边最近任务回到任一最近成片任务。
- 普通图片/视频任务行为不回归。

## P1：创作输入和模型选择

### 4. 让“一键成片”输入表单先讲创作，再讲模型

文件：`frontend/src/components/video.jsx`、`frontend/src/App.css`

现状：

- 模型、LLM、开关、镜头数、时长、比例、语音按字段平铺。
- 用户先看到配置细节，缺少对成片目标和预估的理解。
- 图片模型已不参与现成片流水线，但表单仍提供“生成图片模型”。

修改：

- 删除无效的“生成图片模型”选择和相关 props / 请求字段展示。
- 表单布局调整为：
  1. 视频描述
  2. 输出摘要：总时长、镜头数、比例、预计耗时
  3. 视频模型与语音
  4. “高级选项”：AI 拆镜、分镜模型、每镜时长
- 总时长由 `镜头数 × 每镜时长` 动态计算；只显示估算，不承诺准确成片时长。
- 模型下拉项显示提供商、支持时长/比例和音频能力的简短摘要。
- 无有效视频模型、语音加载中、模型能力未知时，使用明确的 disabled / loading 文案。

验收：

- 首屏在常见笔记本高度可看清描述、输出摘要和提交按钮。
- 用户不再能选择当前流水线未使用的图片模型。
- 用户切模型后，时长和比例被明确更新，不静默改变且没有提示。

### 5. 强化模型目录的信息密度和选择反馈

文件：`frontend/src/components/explore.jsx`、`frontend/src/App.css`

现状：

- 模型卡片只显示类型、ID、版本和提供商。
- 看不出视频模型支持哪些时长、比例、音频或输入能力。
- 选择模型后直接跳回对话，但没有选择确认。

修改：

- 对视频卡片增加能力 chips：时长、比例、音频、参考图输入。
- 对图像卡片显示尺寸或生成数量能力；音频卡片显示对应类型。
- 模型标题和 provider 保持可截断，完整信息放 `title` 或详情区。
- 选中卡片增加明确的“已选”视觉和键盘 focus 样式。
- 点击卡片后回对话时，在 composer 显示一次简短“已选择 model:version”的确认信息。

验收：

- 不用打开参数面板也能比较主要视频模型能力。
- 键盘 Enter/Space 与鼠标点击行为一致。
- 36 个模型卡片时性能和滚动正常。

### 6. 区分普通生成和成片生成的任务语义

文件：`frontend/src/components/chat.jsx`、`frontend/src/App.css`

修改：

- 普通图片/视频生成任务保留聊天气泡。
- 成片任务显示 compact project card：标题“短片生成”、镜头数量、输出比例、任务状态、进入详情动作。
- 不把空 `model:model_version` 渲染为 `:`。
- 输出区对成片优先展示最终 MP4，其次才是镜头视频，避免大量视频卡片淹没结果。

验收：

- 成片任务和单张图片任务在视觉上可立即区分。
- 成片完成后，用户优先看到最终成片而非分镜 URL。

## P1：布局和响应式

### 7. 完成小屏导航和输入区适配

文件：`frontend/src/App.css`、必要时 `chrome.jsx`

现状：

- 900px 以下只把侧栏压缩成窄图标栏。
- 手机上仍占用纵向固定侧栏，内容和输入区空间不足。
- topbar、composer 和 slide-over 各自有窄屏规则，但未作为完整移动流程设计。

修改：

- 在约 640px 以下将侧栏改为底部导航，保留“对话 / 探索 / 日志”。
- 将最近任务和账号状态从手机主导航隐藏，必要信息放在对话顶部或弹层，不占常驻空间。
- 增加底部安全区 padding，避免 iOS home indicator 遮住 composer / 导航。
- topbar 仅保留页面标题、必要动作；“清空”改为图标按钮或 overflow，避免挤压。
- 一键成片面板在手机全宽保留焦点管理和 ESC/返回关闭能力。

验收：

- 375px 宽下没有横向滚动。
- composer、底部导航和提交按钮始终可点。
- 页面滚动时不会被两个 sticky 区域遮住主要内容。

### 8. 调整聊天区的可用高度和空状态

文件：`frontend/src/App.css`

现状：

- `.thread` 用固定的 `calc(100dvh - topbar - 280px)`。
- composer 高度变化、移动端键盘和参数面板都可能令可视聊天区过小。
- welcome 状态过于垂直居中，任务为空时输入动作不够靠前。

修改：

- 让主区域使用 flex 布局分配剩余高度，避免依赖 280px 魔数。
- composer 高度变化时聊天区域自然收缩，而非被覆盖。
- 在小屏缩短 welcome 的顶部留白和建议卡高度。
- 无任务状态将主要输入提示放在可见区，不要求用户先滚动。

验收：

- 桌面、平板、手机上最新消息和 composer 都可同时看到。
- 打开参数面板后不遮挡正在输入的内容。

## P2：可访问性和细节完成度

### 9. 补齐键盘、焦点和语义

文件：`frontend/src/components/video.jsx`、`frontend/src/components/explore.jsx`、`frontend/src/components/chrome.jsx`、`frontend/src/App.css`

修改：

- 打开一键成片 dialog 后将焦点移至标题或描述框；关闭后归还给触发按钮。
- dialog 内实现最小 focus trap，Tab 不离开 panel。
- 所有自定义下拉框支持 Escape 关闭、ArrowUp/ArrowDown 浏览、Enter 确认；若实现成本过高，改回原生 `<select>`。
- `ExploreCard` 改为真实 `<button>`，避免 `div role="button"` 的手工键盘实现。
- 所有纯图像内容提供准确 `alt`，装饰图标保持 `aria-hidden`。
- 对异步提交、生成进度和错误增加 `aria-live="polite"` 区域。

验收：

- 键盘可完成：打开成片面板、填写描述、选择模型、提交、关闭。
- focus ring 清晰且不被容器裁剪。
- 屏幕阅读器能感知任务状态变化。

### 10. 统一状态色、错误信息和 loading 细节

文件：`frontend/src/App.css`、各页面组件

修改：

- 明确使用语义状态色：运行中、成功、警告/部分完成、失败、离线。
- 不仅依赖颜色，状态必须包含图标或文字。
- 模型、语音、任务加载失败时提供可见重试动作。
- 对空模型列表、无日志、无作品、无搜索结果使用一致的空状态组件样式。
- 为动画遵守 `prefers-reduced-motion`；运行中 dot、进度条、slide-over 需要静态降级。

验收：

- 失败、警告和成功在色弱模式下也能区分。
- 所有网络加载页面都有明确的 loading / retry / empty 状态。

## 推荐实施顺序

1. P0-1、P0-2、P0-3：先让成片任务状态与入口清楚可靠。
2. P1-4、P1-6：清理成片表单和聊天任务语义。
3. P1-7、P1-8：处理移动端与布局高度。
4. P1-5：丰富模型目录。
5. P2-9、P2-10：补可访问性和视觉完成度。

## 每个阶段的验证

- `npm run build` 必须通过。
- 使用浏览器在 1440px、900px、640px、375px 四种宽度检查对话、探索、日志、一键成片表单、成片进度和成片失败状态。
- 使用键盘完成一次普通生成与一次一键成片提交。
- 运行至少一次任务成功、一次任务部分失败或失败的 UI 验证。
- 不因视觉优化修改后端 API 字段或任务状态语义。
